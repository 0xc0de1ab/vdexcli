name: "[03] Release"

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - name: Validate release ref and source
        run: |
          if [[ ! "$GITHUB_REF" =~ ^refs/tags/v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Release workflow must run from a vMAJOR.MINOR.PATCH tag"
            exit 1
          fi
          git fetch --no-tags origin main
          tag_commit=$(git rev-list -n 1 "$GITHUB_REF_NAME")
          if [ "$(git rev-parse HEAD)" != "$tag_commit" ]; then
            echo "::error::Checked-out commit does not match the release tag"
            exit 1
          fi
          if ! git merge-base --is-ancestor "$tag_commit" origin/main; then
            echo "::error::Release tag commit is not reachable from origin/main"
            exit 1
          fi
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version-file: go.mod
      - name: Install zstd
        run: sudo apt-get install -y zstd
      - name: Run tests
        run: go test -v -count=1 ./...

  release:
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            suffix: ''
          - goos: linux
            goarch: arm64
            suffix: ''
          - goos: darwin
            goarch: amd64
            suffix: ''
          - goos: darwin
            goarch: arm64
            suffix: ''
          - goos: windows
            goarch: amd64
            suffix: '.exe'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version-file: go.mod
      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: make build GOOS=$GOOS GOARCH=$GOARCH
      - name: Package
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          SUFFIX: ${{ matrix.suffix }}
        run: |
          cd build/$GOOS-$GOARCH/release
          if [ -n "$SUFFIX" ] && [ ! -f "vdexcli${SUFFIX}" ]; then
            mv vdexcli "vdexcli${SUFFIX}"
          fi
          ARCHIVE="vdexcli-${GITHUB_REF_NAME}-${GOOS}-${GOARCH}"
          if [ "$GOOS" = "windows" ]; then
            zip "${ARCHIVE}.zip" vdexcli${SUFFIX}
          else
            tar czf "${ARCHIVE}.tar.gz" vdexcli${SUFFIX}
          fi
          ls -lh ${ARCHIVE}.*
      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: vdexcli-${{ matrix.goos }}-${{ matrix.goarch }}
          path: build/${{ matrix.goos }}-${{ matrix.goarch }}/release/vdexcli-*
          retention-days: 90

  publish:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      release_url: ${{ steps.github_release.outputs.url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: dist
          merge-multiple: true
      - name: List artifacts
        run: ls -lhR dist/
      - name: Generate checksums
        run: cd dist && sha256sum vdexcli-* > vdexcli-checksums.txt && cat vdexcli-checksums.txt
      - name: Create GitHub Release
        id: github_release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3
        with:
          generate_release_notes: true
          files: dist/*

  summary:
    name: "summary"
    if: always()
    needs: [test, release, publish]
    runs-on: ubuntu-latest
    env:
      TEST_RESULT: ${{ needs.test.result }}
      BUILD_RESULT: ${{ needs.release.result }}
      PUBLISH_RESULT: ${{ needs.publish.result }}
      RELEASE_URL: ${{ needs.publish.outputs.release_url }}
    steps:
      - name: Write workflow summary
        run: |
          short_sha="${GITHUB_SHA::7}"
          release_url="${RELEASE_URL:-unavailable}"
          {
            echo "## Release"
            echo
            echo "| Stage | Result |"
            echo "| --- | --- |"
            printf '| Tests | `%s` |\n' "$TEST_RESULT"
            printf '| Build and package | `%s` |\n' "$BUILD_RESULT"
            printf '| Publish | `%s` |\n' "$PUBLISH_RESULT"
            echo
            echo "### Artifacts"
            printf -- '- Tag: `%s`\n' "$GITHUB_REF_NAME"
            printf -- '- Targets: `linux/amd64`, `linux/arm64`, `darwin/amd64`, `darwin/arm64`, `windows/amd64`\n'
            printf -- '- Checksums: `vdexcli-checksums.txt`\n'
            printf -- '- Release: %s\n' "$release_url"
            echo
            echo "### Run"
            printf -- '- Event: `%s`\n' "$GITHUB_EVENT_NAME"
            printf -- '- Commit: [`%s`](%s/%s/commit/%s)\n' "$short_sha" "$GITHUB_SERVER_URL" "$GITHUB_REPOSITORY" "$GITHUB_SHA"
          } >> "$GITHUB_STEP_SUMMARY"
